version: '3.8'

services:
  grid-box-toggle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grid-box-toggle-app
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

